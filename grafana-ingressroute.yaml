apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: grafana-ingressroute
  namespace: monitoring
spec:
  entryPoints:
    - http
  routes:
  - match: Host(`grafanaservice.local`) && PathPrefix(`/`)
    kind: Rule
    services:
    - name: grafana
      port: 3000
